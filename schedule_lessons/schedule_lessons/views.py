from django.shortcuts import render

# TODO: Create a method that will return the 404 html page with 2 paramters:
# request and exception. You can return the html page using the render method,
# view the documentation:
# https://docs.djangoproject.com/en/2.0/topics/http/shortcuts/#render
# The html page that you'll need to return is 404.html, and it's in the errors
# subdirectory (look at the templates folder to figure out hieracy)
